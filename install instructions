apt-get update
apt-get install upgrade
apt-get install git
git clone https://github.com/beta-things/tcs_case_ui.git .

sudo ./displaylink-driver-5.6.1-59.184.run
reboot

#power settings
#setup docker 
#apt-get install docker-compose 
docker build -t tekskil_case_ui
#install kyle's stuff
#install service files 
#enable services 
apt-get install chromium-browser 
gnome-session-properties
chromium-browser --kiosk file:///home/tch/Documents/loading.html
#install chromium black in extensions in dev mode 
#set background to screenshot 
sudo mkfifo compipe in Documents/tcs_ui_pipe

